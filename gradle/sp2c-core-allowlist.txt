# SP2c :core immutability guard allowlist (Issue #823, #822 C10/P9).
#
# One repo-relative path per line (e.g. core/src/commonMain/kotlin/.../Foo.kt), exempting
# it from checkCoreUntouchedBySp2c (root build.gradle.kts). Lines starting with '#' and
# blank lines are ignored.
#
# Empty at creation, on purpose: the guard does not forbid :core changes during the SP2c
# redesign, it forbids *unnoticed* ones. Adding a line here requires an explicit owner
# decision recorded in the body of the PR that adds it — see docs/KOTLIN_STYLE_GUIDE.md
# ("SP2c :core Immutability Guard") for the full exception process.
#
# Issue #906 — fix: completedTrains counts journeys, not route releases
# Decision: orthogonal metrics-correctness fix, not a Goal-10/SP2c change.
# Changes are limited to DefaultMetricsCollectionService (bug fix: add movedTrains guard)
# and MetricsSnapshot KDoc update.  No simulation logic, no dispatcher code, no
# kDisco internals touched.
core/src/commonMain/kotlin/cz/vutbr/fit/interlockSim/sim/metrics/DefaultMetricsCollectionService.kt
core/src/commonMain/kotlin/cz/vutbr/fit/interlockSim/sim/metrics/MetricsSnapshot.kt
core/src/commonTest/kotlin/cz/vutbr/fit/interlockSim/sim/metrics/DefaultMetricsCollectionServiceTest.kt

{
  "type": "object",
  "required": ["actions"],
  "properties": {
    "actions": {
      "type": "array",
      "maxItems": 3,
      "items": {
        "type": "object",
        "required": ["action"],
        "properties": {
          "action": {
            "type": "string",
            "enum": ["approve_train", "request_route", "cancel_route", "no_op"]
          },
          "trainId": { "type": "string" },
          "fromEndpointName": { "type": "string" },
          "toEndpointName": { "type": "string" },
          "reason": { "type": "string" }
        }
      }
    }
  }
}